# 分步实验执行提示词

## 1. 文档目的

本文档把 ETTH1 周期波峰预测实验拆成可逐步执行的小任务。每一步都可以单独复制给一个新的智能体执行，因此每一步提示词都必须自包含：

```text
1. 固定工作目录
2. 固定输入文件路径
3. 固定输出文件路径
4. 本步不得依赖“上一步记忆”
5. 验收标准必须写在提示词内部
```

## 2. 全局路径约定

当前已实现项目位于：

```text
WSL 路径：/home/wmb/test/波峰波谷预测
Windows 路径：\\wsl.localhost\Ubuntu-24.04\home\wmb\test\波峰波谷预测
```

当前已有代码与数据：

```text
原始数据：ETTh1.csv
已实现数据集代码：src/wave_dataset/
已生成数据集：数据集/ETTH1_pred14_seq4/
已生成可视化：数据集可视化/ETTH1_pred14_seq4/
实现文档：实现文档/
```

后续规则基线、特征矩阵、模型训练、评估等代码尚未在当前项目中确认实现。建议新建：

```text
[待确认] 新实验代码目录：src/wave_experiments/
[待确认] 新实验输出目录：实验输出/
```

如你希望继续沿用 `代码实现/` 目录，请先确认；当前实际代码结构不是 `代码实现/`，而是 `src/wave_dataset/`。

运行环境：

```bash
cd /home/wmb/test/波峰波谷预测
PYTHONPATH=src /home/wmb/anaconda3/envs/Ada-MSHyper/bin/python -m unittest discover -s tests -v
```

## 3. 当前已知数据集路径

每个新智能体执行时都应直接读取这些固定路径，不要写“基于第 1 步找到的数据集”。

```text
完整自然日清单：
数据集/ETTH1_pred14_seq4/完整自然日清单.csv

日周期峰值标签：
数据集/ETTH1_pred14_seq4/日周期峰值标签.csv

全部变量长表：
数据集/ETTH1_pred14_seq4/长表/峰谷预测长表_seq96_pred336_全部变量.csv

全部变量长表 split：
数据集/ETTH1_pred14_seq4/长表/峰谷预测长表_seq96_pred336_全部变量_训练.csv
数据集/ETTH1_pred14_seq4/长表/峰谷预测长表_seq96_pred336_全部变量_验证.csv
数据集/ETTH1_pred14_seq4/长表/峰谷预测长表_seq96_pred336_全部变量_测试.csv

拐点标签目录：
数据集/ETTH1_pred14_seq4/拐点标签/

可视化目录：
数据集可视化/ETTH1_pred14_seq4/
```

## 4. 总体执行顺序

| 阶段 | 步骤 | 内容 |
| --- | --- | --- |
| A | 第 1-2 步 | 检查已生成数据集，生成验证报告 |
| B | 第 3-6 步 | 实现并评估规则基线 |
| C | 第 7-8 步 | 生成残差标签和统一特征表 |
| D | 第 9-10 步 | 实现 LightGBM 第一版 |
| E | 第 11-12 步 | 实现 XGBoost 第一版 |
| F | 第 13-14 步 | 汇总结果并判断模型是否学到东西 |
| G | 第 15-16 步 | 做 `seq_len` 和 `pred_len` 递进实验 |
| H | 第 17-18 步 | 做第二版小波增强实验 |

## 第 1 步：检查已生成数据集

### 本步目标

确认当前已生成的数据集、拐点标签和可视化是否满足后续规则基线、残差学习和模型训练要求。

### 完整提示词

```text
请在 WSL 的 `/home/wmb/test/波峰波谷预测` 目录中工作，运行环境使用 `/home/wmb/anaconda3/envs/Ada-MSHyper/bin/python`，并设置 `PYTHONPATH=src`。

不要重新生成数据集，不要修改 `ETTh1.csv`，不要修改 `数据集/ETTH1_pred14_seq4/` 中的已有 CSV。请只做只读检查，并输出检查摘要。

固定检查对象：
1. `数据集/ETTH1_pred14_seq4/完整自然日清单.csv`
2. `数据集/ETTH1_pred14_seq4/日周期峰值标签.csv`
3. `数据集/ETTH1_pred14_seq4/长表/峰谷预测长表_seq96_pred336_全部变量.csv`
4. `数据集/ETTH1_pred14_seq4/长表/峰谷预测长表_seq96_pred336_全部变量_训练.csv`
5. `数据集/ETTH1_pred14_seq4/长表/峰谷预测长表_seq96_pred336_全部变量_验证.csv`
6. `数据集/ETTH1_pred14_seq4/长表/峰谷预测长表_seq96_pred336_全部变量_测试.csv`
7. `数据集/ETTH1_pred14_seq4/拐点标签/` 下 7 个变量的拐点 CSV
8. `数据集可视化/ETTH1_pred14_seq4/` 下 7 个变量的波峰波谷图和拐点图

检查内容：
1. 是否覆盖 7 个目标变量：HUFL,HULL,MUFL,MULL,LUFL,LULL,OT。
2. 长表是否包含：样本ID、预测起点日期、目标变量、预测天数、目标峰值、目标峰值残差、目标峰值小时、基线峰值、数据集划分。
3. `预测天数` 是否覆盖 1 到 14。
4. `数据集划分` 是否覆盖 训练、验证、测试。
5. 关键字段是否有缺失。
6. 特征列是否包含 7 个变量前缀。
7. 拐点标签是否每个变量 1 个文件，字段是否包含 日期、原始值、平滑值、拐点类型、拐点显著性、拐点阈值、有效标记。
8. 可视化是否每个变量至少包含 1 张波峰波谷图和 1 张拐点图。

输出：
1. 生成 `实现文档/第1步_数据集文件检查摘要.md`。
2. 报告中必须列出检查命令、检查文件、行数、列数、变量分布、horizon 分布、split 分布、缺失值统计、可视化文件数量。

验收标准：
1. 明确列出已检查的数据集文件路径。
2. 明确说明 7 个变量是否全部覆盖。
3. 明确说明 horizon 是否覆盖 1 到 14。
4. 明确说明 train/val/test 行数。
5. 明确说明关键字段是否有缺失。
6. 明确说明特征是否包含 7 个变量历史输入。
7. 明确说明拐点标签和可视化是否齐全。
8. 不修改原始数据和已生成数据集。
```

## 第 2 步：生成数据集验证报告

### 本步目标

对固定长表数据集做更严格的数据质量验证，形成后续实验的可信输入。

### 完整提示词

```text
请在 `/home/wmb/test/波峰波谷预测` 工作，使用 `PYTHONPATH=src /home/wmb/anaconda3/envs/Ada-MSHyper/bin/python`。

固定输入：
`数据集/ETTH1_pred14_seq4/长表/峰谷预测长表_seq96_pred336_全部变量.csv`

[待确认] 请将新增验证脚本放在：
`src/wave_experiments/datasets/validate_peak_dataset.py`

[待确认] 输出到：
`实验输出/reports/数据集验证报告.md`
`实验输出/results/dataset_validation_summary.csv`

验证内容：
1. 检查 `数据集划分` 是否只包含 训练、验证、测试。
2. 检查每条样本是否包含完整的 目标变量、预测天数、目标峰值、目标峰值小时。
3. 检查 `预测天数` 是否覆盖 1 到 14。
4. 检查 `目标变量` 是否覆盖 7 个变量。
5. 检查 `目标峰值小时` 是否只在 0 到 23 范围内。
6. 检查 `目标峰值` 是否为数值。
7. 检查特征列是否存在 NaN 或 Inf。
8. 检查是否存在明显信息泄露字段，例如未来真实目标峰值被放入输入特征。
9. 检查训练、验证、测试的预测起点日期是否按时间顺序划分。

运行命令：
`PYTHONPATH=src /home/wmb/anaconda3/envs/Ada-MSHyper/bin/python src/wave_experiments/datasets/validate_peak_dataset.py`

验收标准：
1. 验证脚本存在。
2. `数据集验证报告.md` 存在。
3. `dataset_validation_summary.csv` 存在。
4. 报告明确 horizon 覆盖 1 到 14。
5. 报告明确覆盖 7 个目标变量。
6. 报告明确 `目标峰值小时` 全部在 0 到 23。
7. 报告明确列出疑似泄露字段，或说明未发现明显泄露。
8. 报告明确是否允许进入第 3 步。
```

## 第 3 步：实现规则基线的数据加载

### 本步目标

建立规则基线所需的数据加载模块，统一读取训练、验证、测试样本。

### 完整提示词

```text
请在 `/home/wmb/test/波峰波谷预测` 工作，使用 `PYTHONPATH=src /home/wmb/anaconda3/envs/Ada-MSHyper/bin/python`。

固定输入：
`数据集/ETTH1_pred14_seq4/长表/峰谷预测长表_seq96_pred336_全部变量.csv`

新增代码路径：
`src/wave_experiments/baselines/load_baseline_data.py`

要求：
1. 提供函数 `load_peak_dataset()`，返回 train_df、val_df、test_df。
2. 按 `数据集划分` 分成 训练、验证、测试。
3. 保留 ID 字段、标签字段、基线字段、日历字段、历史统计特征字段。
4. 函数内部固定读取上述长表路径，也允许通过参数覆盖路径。
5. 命令行运行后打印 train/val/test 形状、字段数量、目标变量分布和 horizon 分布。
6. 不把未来真实标签字段当作特征列返回。

运行命令：
`PYTHONPATH=src /home/wmb/anaconda3/envs/Ada-MSHyper/bin/python src/wave_experiments/baselines/load_baseline_data.py`

验收标准：
1. `load_baseline_data.py` 存在。
2. `load_peak_dataset()` 存在并可导入。
3. 运行脚本能输出 train、val、test 行数。
4. 输出中包含 7 个变量分布和 horizon 分布。
5. 明确区分标签列和特征列。
```

## 第 4 步：实现 peak_value 多种规则基线

### 本步目标

实现多个 `peak_value` 规则基线，为后续残差学习提供参考。

### 完整提示词

```text
请在 `/home/wmb/test/波峰波谷预测` 工作。

固定输入：
`数据集/ETTH1_pred14_seq4/长表/峰谷预测长表_seq96_pred336_全部变量.csv`

新增代码：
`src/wave_experiments/baselines/peak_value_baselines.py`

输出：
`实验输出/results/baselines/peak_value_baseline_predictions.csv`
`实验输出/results/baselines/peak_value_baseline_metrics.csv`

需要实现以下基线：
1. `mean_last_4`：过去 4 天目标变量历史峰值均值。
2. `weighted_mean_last_4`：过去 4 天历史峰值加权均值，权重从旧到新为 0.1,0.2,0.3,0.4。
3. `cycle_mod_4`：未来第 h 天使用过去 4 天中 h mod 4 对应的历史日峰值。
4. `weekday_horizon_mean`：训练集中同目标变量、星期、预测天数的平均目标峰值。

要求：
1. 为验证集和测试集生成预测。
2. 输出字段包含 样本ID、目标变量、预测天数、目标峰值、baseline_name、baseline_peak_value。
3. 计算 MAE、RMSE、sMAPE。
4. 按全局、目标变量、预测天数、目标变量+预测天数四个层级评估。

运行命令：
`PYTHONPATH=src /home/wmb/anaconda3/envs/Ada-MSHyper/bin/python src/wave_experiments/baselines/peak_value_baselines.py`

验收标准：
1. 4 种基线全部实现。
2. 预测表存在并包含验证集、测试集。
3. 指标表存在并包含 MAE、RMSE、sMAPE。
4. 指标表包含全局、变量、horizon、变量+horizon 分层。
5. `baseline_peak_value` 无缺失。
```

## 第 5 步：实现 peak_hour 规则基线

### 本步目标

为峰值出现小时建立简单时间基线。

### 完整提示词

```text
请在 `/home/wmb/test/波峰波谷预测` 工作。

固定输入：
`数据集/ETTH1_pred14_seq4/长表/峰谷预测长表_seq96_pred336_全部变量.csv`

[待确认] 新增代码：
`src/wave_experiments/baselines/peak_hour_baselines.py`

[待确认] 输出：
`实验输出/results/baselines/peak_hour_baseline_predictions.csv`
`实验输出/results/baselines/peak_hour_baseline_metrics.csv`

需要实现以下基线：
1. `mode_last_4`：过去 4 天峰值小时众数，并列时取较早小时。
2. `median_last_4`：过去 4 天峰值小时中位数，四舍五入为整数小时。
3. `global_mode`：训练集中同目标变量最常见峰值小时。
4. `weekday_mode`：训练集中同目标变量、星期最常见峰值小时。

评估指标：
1. 普通小时误差。
2. 环形小时误差。
3. Top-1 accuracy。
4. ±1h 命中率。
5. ±2h 命中率。

运行命令：
`PYTHONPATH=src /home/wmb/anaconda3/envs/Ada-MSHyper/bin/python src/wave_experiments/baselines/peak_hour_baselines.py`

验收标准：
1. 4 种基线全部实现。
2. 所有预测小时在 0 到 23。
3. 指标表包含普通误差、环形误差、Top-1、±1h、±2h。
4. 指标表包含全局、变量、horizon、变量+horizon 分层。
```

## 第 6 步：评估并选择最佳规则基线

### 本步目标

整合 `peak_value` 和 `peak_hour` 规则基线，选择后续残差学习使用的最佳基线。

### 完整提示词

```text
请在 `/home/wmb/test/波峰波谷预测` 工作。

固定输入：
`实验输出/results/baselines/peak_value_baseline_predictions.csv`
`实验输出/results/baselines/peak_value_baseline_metrics.csv`
`实验输出/results/baselines/peak_hour_baseline_predictions.csv`
`实验输出/results/baselines/peak_hour_baseline_metrics.csv`

[待确认] 新增代码：
`src/wave_experiments/baselines/select_best_baselines.py`

[待确认] 输出：
`实验输出/results/baselines/best_peak_value_baseline_by_group.csv`
`实验输出/results/baselines/best_peak_hour_baseline_by_group.csv`
`实验输出/results/baselines/dataset_with_selected_baselines.csv`
`实验输出/reports/规则基线总结.md`

要求：
1. 对 peak_value，按目标变量+预测天数，在验证集上选择 MAE 最低的基线。
2. 对 peak_hour，按目标变量+预测天数，在验证集上选择 ±2h 命中率最高的基线；并列时选择平均小时误差更低者。
3. 输出每行样本对应的 `baseline_peak`。
4. 输出后续残差学习可直接使用的数据表。
5. 报告中说明每个变量、每个 horizon 使用了哪个最佳基线。

运行命令：
`PYTHONPATH=src /home/wmb/anaconda3/envs/Ada-MSHyper/bin/python src/wave_experiments/baselines/select_best_baselines.py`

验收标准：
1. peak_value 和 peak_hour 都有最佳基线选择表。
2. 选择依据明确写入报告。
3. `dataset_with_selected_baselines.csv` 存在。
4. `baseline_peak` 无缺失。
5. `规则基线总结.md` 存在。
```

## 第 7 步：生成 peak_value 残差标签

### 本步目标

基于最佳规则基线生成残差学习目标。

### 完整提示词

```text
请在 `/home/wmb/test/波峰波谷预测` 工作。

固定输入：
`实验输出/results/baselines/dataset_with_selected_baselines.csv`

[待确认] 新增代码：
`src/wave_experiments/features/build_residual_targets.py`

[待确认] 输出：
`实验输出/results/features/dataset_with_residual_targets.csv`
`实验输出/reports/残差标签报告.md`

要求：
1. 新增 `target_peak_residual = target_peak_value - baseline_peak`。
2. 检查 `baseline_peak + target_peak_residual` 是否能还原 `target_peak_value`。
3. 保留模型训练所需字段。
4. 生成残差分布报告，包括每个目标变量、预测天数的均值、标准差、最小值、最大值。

运行命令：
`PYTHONPATH=src /home/wmb/anaconda3/envs/Ada-MSHyper/bin/python src/wave_experiments/features/build_residual_targets.py`

验收标准：
1. `target_peak_residual` 存在。
2. 还原误差为 0 或在浮点容忍范围内。
3. 残差列无 NaN。
4. `残差标签报告.md` 存在。
```

## 第 8 步：整理模型训练特征表

### 本步目标

生成 LightGBM 和 XGBoost 共用的训练特征表。

### 完整提示词

```text
请在 `/home/wmb/test/波峰波谷预测` 工作。

固定输入：
`实验输出/results/features/dataset_with_residual_targets.csv`

[待确认] 新增代码：
`src/wave_experiments/features/build_model_matrix.py`

[待确认] 输出：
`实验输出/results/features/model_matrix_seq96_pred336.csv`
`实验输出/results/features/feature_columns_seq96_pred336.txt`
`实验输出/reports/模型特征表报告.md`

要求：
1. 明确划分 ID 字段、标签字段、基线字段、特征字段。
2. 特征字段必须全部为数值。
3. 删除或排除未来真实标签字段，避免信息泄露。
4. 训练、验证、测试三个 split 的特征列必须完全一致。
5. 输出完整特征矩阵和特征列清单。

运行命令：
`PYTHONPATH=src /home/wmb/anaconda3/envs/Ada-MSHyper/bin/python src/wave_experiments/features/build_model_matrix.py`

验收标准：
1. `model_matrix_seq96_pred336.csv` 存在。
2. `feature_columns_seq96_pred336.txt` 存在。
3. 所有特征列可转为数值。
4. 无 NaN 和 Inf，或报告中说明明确填充策略。
5. 标签字段不进入特征列。
```

## 第 9 步：实现 LightGBM peak_value 残差回归

### 本步目标

训练 LightGBM 回归模型，预测 `target_peak_residual`，再还原为 `pred_peak_value`。

### 完整提示词

```text
请在 `/home/wmb/test/波峰波谷预测` 工作。

固定输入：
`实验输出/results/features/model_matrix_seq96_pred336.csv`
`实验输出/results/features/feature_columns_seq96_pred336.txt`

[待确认] 新增代码：
`src/wave_experiments/models/train_lightgbm_peak_value.py`

要求：
1. 每个目标变量单独训练一个模型，共 7 个回归模型。
2. 使用训练集训练，验证集 early stopping，测试集最终评估。
3. 模型目标为 `target_peak_residual`。
4. 预测后计算 `pred_peak_value = baseline_peak + pred_peak_residual`。
5. 输出验证集和测试集预测结果。
6. 输出全局、按变量、按 horizon、按变量+horizon 的 MAE、RMSE、sMAPE。
7. 与最佳规则基线比较，计算 improvement。
8. 保存模型和特征重要性。

运行命令：
`PYTHONPATH=src /home/wmb/anaconda3/envs/Ada-MSHyper/bin/python src/wave_experiments/models/train_lightgbm_peak_value.py`

验收标准：
1. 生成 7 个 LightGBM 回归模型。
2. 预测结果包含 `pred_peak_residual` 和 `pred_peak_value`。
3. 指标包含 MAE、RMSE、sMAPE、improvement。
4. 明确是否超过最佳规则基线。
5. 输出特征重要性表。
```

## 第 10 步：实现 LightGBM peak_hour 分类

### 本步目标

训练 LightGBM 24 分类模型预测 `peak_hour`。

### 完整提示词

```text
请在 `/home/wmb/test/波峰波谷预测` 工作。

固定输入：
`实验输出/results/features/model_matrix_seq96_pred336.csv`
`实验输出/results/features/feature_columns_seq96_pred336.txt`

[待确认] 新增代码：
`src/wave_experiments/models/train_lightgbm_peak_hour.py`

要求：
1. 每个目标变量单独训练一个 24 分类模型，共 7 个模型。
2. 使用训练集训练，验证集 early stopping，测试集最终评估。
3. 目标为 `target_peak_hour`，取值 0 到 23。
4. 输出 `pred_peak_hour`。
5. 评估普通小时误差、环形小时误差、Top-1 accuracy、±1h 命中率、±2h 命中率。
6. 与最佳 peak_hour 规则基线比较。
7. 保存模型和特征重要性。

运行命令：
`PYTHONPATH=src /home/wmb/anaconda3/envs/Ada-MSHyper/bin/python src/wave_experiments/models/train_lightgbm_peak_hour.py`

验收标准：
1. 生成 7 个 LightGBM 分类模型。
2. `pred_peak_hour` 全部在 0 到 23。
3. 指标包含普通误差、环形误差、Top-1、±1h、±2h。
4. 明确是否超过 peak_hour 规则基线。
```

## 第 11 步：实现 XGBoost peak_value 残差回归

### 本步目标

使用与 LightGBM 相同的数据和特征训练 XGBoost 回归模型。

### 完整提示词

```text
请在 `/home/wmb/test/波峰波谷预测` 工作。

固定输入：
`实验输出/results/features/model_matrix_seq96_pred336.csv`
`实验输出/results/features/feature_columns_seq96_pred336.txt`

[待确认] 新增代码：
`src/wave_experiments/models/train_xgboost_peak_value.py`

要求：
1. 使用与 LightGBM 完全相同的数据和特征。
2. 每个目标变量单独训练一个模型，共 7 个回归模型。
3. 目标为 `target_peak_residual`。
4. 预测后还原 `pred_peak_value = baseline_peak + pred_peak_residual`。
5. 输出与 LightGBM 同格式的预测表、评估表、特征重要性。
6. 与最佳规则基线比较 improvement。

运行命令：
`PYTHONPATH=src /home/wmb/anaconda3/envs/Ada-MSHyper/bin/python src/wave_experiments/models/train_xgboost_peak_value.py`

验收标准：
1. 生成 7 个 XGBoost 回归模型。
2. 使用与 LightGBM 相同的数据和特征。
3. 预测表字段与 LightGBM peak_value 预测表一致。
4. 指标包含 MAE、RMSE、sMAPE、improvement。
```

## 第 12 步：实现 XGBoost peak_hour 分类

### 本步目标

使用 XGBoost 训练 24 分类模型预测 `peak_hour`。

### 完整提示词

```text
请在 `/home/wmb/test/波峰波谷预测` 工作。

固定输入：
`实验输出/results/features/model_matrix_seq96_pred336.csv`
`实验输出/results/features/feature_columns_seq96_pred336.txt`

[待确认] 新增代码：
`src/wave_experiments/models/train_xgboost_peak_hour.py`

要求：
1. 使用与 LightGBM peak_hour 完全相同的数据和特征。
2. 每个目标变量单独训练一个 24 分类模型，共 7 个模型。
3. 目标为 `target_peak_hour`。
4. 输出 `pred_peak_hour`。
5. 评估普通小时误差、环形小时误差、Top-1 accuracy、±1h 命中率、±2h 命中率。
6. 与 LightGBM 和 peak_hour 规则基线比较。

运行命令：
`PYTHONPATH=src /home/wmb/anaconda3/envs/Ada-MSHyper/bin/python src/wave_experiments/models/train_xgboost_peak_hour.py`

验收标准：
1. 生成 7 个 XGBoost 分类模型。
2. `pred_peak_hour` 全部在 0 到 23。
3. 预测表字段与 LightGBM peak_hour 预测表一致。
4. 指标包含普通误差、环形误差、Top-1、±1h、±2h。
```

## 第 13 步：汇总 LightGBM、XGBoost 和规则基线结果

### 本步目标

把 E00、E01、E02 的结果汇总成统一实验对比表。

### 完整提示词

```text
请在 `/home/wmb/test/波峰波谷预测` 工作。

固定输入：
1. `实验输出/results/baselines/` 下规则基线评估结果。
2. `实验输出/results/lightgbm/` 下 LightGBM 评估结果。
3. `实验输出/results/xgboost/` 下 XGBoost 评估结果。

[待确认] 新增代码：
`src/wave_experiments/evaluation/compare_experiments.py`

[待确认] 输出：
`实验输出/results/实验对比总表.csv`
`实验输出/reports/第一版实验结果总结.md`

要求：
1. 汇总全局结果。
2. 汇总按目标变量的结果。
3. 汇总按 horizon 的结果。
4. 汇总按目标变量+horizon 的结果。
5. 标记每个模型是否超过最佳规则基线。
6. 判断当前最佳模型。
7. 生成 Markdown 总结报告。

运行命令：
`PYTHONPATH=src /home/wmb/anaconda3/envs/Ada-MSHyper/bin/python src/wave_experiments/evaluation/compare_experiments.py`

验收标准：
1. `实验对比总表.csv` 存在。
2. `第一版实验结果总结.md` 存在。
3. 包含全局、变量、horizon、变量+horizon 分层。
4. 明确当前最佳模型和原因。
```

## 第 14 步：诊断模型是否真正学到东西

### 本步目标

从残差相关性、分布、变量覆盖和 horizon 覆盖判断模型是否真正有效。

### 完整提示词

```text
请在 `/home/wmb/test/波峰波谷预测` 工作。

固定输入：
`实验输出/results/实验对比总表.csv`
以及 LightGBM、XGBoost 的预测结果明细。

[待确认] 新增代码：
`src/wave_experiments/evaluation/diagnose_model_learning.py`

[待确认] 输出：
`实验输出/reports/模型学习诊断报告.md`

要求：
1. 检查 LightGBM 和 XGBoost 的 pred_residual 与 true_residual 相关性。
2. 检查预测 peak_value 分布是否相对真实分布明显塌缩。
3. 检查每个目标变量是否多数 horizon 优于规则基线。
4. 检查每个 horizon 是否多数目标变量优于规则基线。
5. 检查 peak_hour 是否至少在 ±2h 命中率上优于规则基线。
6. 输出诊断结论：保持当前方案、增加 seq_len、缩短 pred_len、进入小波第二版。

运行命令：
`PYTHONPATH=src /home/wmb/anaconda3/envs/Ada-MSHyper/bin/python src/wave_experiments/evaluation/diagnose_model_learning.py`

验收标准：
1. `模型学习诊断报告.md` 存在。
2. 按模型和变量给出残差相关性。
3. 比较真实峰值和预测峰值标准差、分位数。
4. 明确下一步建议。
```

## 第 15 步：增加 seq_len 实验

### 本步目标

在 `pred_len=336h` 不变的情况下，测试更长历史输入是否提升效果。

### 完整提示词

```text
请在 `/home/wmb/test/波峰波谷预测` 工作。

目标实验：
1. seq_len=192h, pred_len=336h
2. seq_len=336h, pred_len=336h
3. seq_len=672h, pred_len=336h

[待确认] 新增编排代码：
`src/wave_experiments/evaluation/run_seq_len_experiments.py`

要求：
1. 每个 seq_len 都重新生成或加载对应数据集。
2. 每个 seq_len 都重新运行规则基线、残差标签、特征矩阵、LightGBM、XGBoost、评估。
3. 所有输出文件名必须包含 seq_len 和 pred_len，避免覆盖第一版结果。
4. 汇总三个 seq_len 与 seq_len=96h 的结果。
5. 判断增长输入是否有效。

运行命令：
`PYTHONPATH=src /home/wmb/anaconda3/envs/Ada-MSHyper/bin/python src/wave_experiments/evaluation/run_seq_len_experiments.py`

验收标准：
1. 完成 192h、336h、672h 三组实验。
2. 不覆盖 seq96 的结果。
3. 输出不同 seq_len 的 MAE 和 improvement 对比。
4. 明确判断是否应采用更长历史。
```

## 第 16 步：缩短 pred_len 实验

### 本步目标

如果未来 14 天效果差，测试未来 8 天和未来 4 天是否更可预测。

### 完整提示词

```text
请在 `/home/wmb/test/波峰波谷预测` 工作。

固定前提：
使用第 14 步诊断报告中建议的最佳 seq_len。如果没有最佳 seq_len 结论，则使用 seq_len=96h。

目标实验：
1. pred_len=192h，即未来 8 天
2. pred_len=96h，即未来 4 天

[待确认] 新增编排代码：
`src/wave_experiments/evaluation/run_pred_len_experiments.py`

要求：
1. 保持特征、模型和评估方式与第一版一致。
2. 每个 pred_len 都重新生成对应样本或加载对应样本。
3. 运行规则基线、残差学习、LightGBM、XGBoost。
4. 对比 pred_len=336h、192h、96h 的结果。
5. 判断是否保留未来 14 天任务，或改为短期预测。

运行命令：
`PYTHONPATH=src /home/wmb/anaconda3/envs/Ada-MSHyper/bin/python src/wave_experiments/evaluation/run_pred_len_experiments.py`

验收标准：
1. 完成 192h 和 96h 两组实验。
2. 与 336h 结果同表比较。
3. 明确判断短期预测是否明显更可靠。
```

## 第 17 步：生成小波特征

### 本步目标

为第二版实验生成只基于历史输入窗口的小波特征。

### 完整提示词

```text
请在 `/home/wmb/test/波峰波谷预测` 工作。

固定输入：
使用当前最佳版本的模型矩阵。如果尚未确定最佳版本，则使用：
`实验输出/results/features/model_matrix_seq96_pred336.csv`

[待确认] 新增代码：
`src/wave_experiments/features/build_wavelet_features.py`

[待确认] 输出：
`实验输出/results/features/model_matrix_seq96_pred336_wavelet.csv`
`实验输出/results/features/wavelet_feature_columns_seq96_pred336.txt`
`实验输出/reports/小波特征生成报告.md`

要求：
1. 对每个样本，只使用预测起点之前的 seq_len 小时历史窗口。
2. 对 7 个变量分别计算小波特征。
3. 默认使用离散小波变换，优先使用 `db4` 小波基。
4. 生成低频趋势最后值、低频趋势斜率、高频能量、高频能量占比、去噪序列最大值、去噪序列最后值、细节系数绝对值均值、细节系数绝对值最大值。
5. 严禁对完整数据一次性小波分解后再切样本。
6. 输出带小波特征的新模型矩阵。

运行命令：
`PYTHONPATH=src /home/wmb/anaconda3/envs/Ada-MSHyper/bin/python src/wave_experiments/features/build_wavelet_features.py`

验收标准：
1. 小波特征表存在。
2. 小波特征列清单存在。
3. 报告明确说明每个样本只使用历史窗口计算。
4. 小波特征无 NaN/Inf，或有明确填充策略。
```

## 第 18 步：运行第二版小波增强模型

### 本步目标

加入小波特征后重新训练 LightGBM 和 XGBoost，并与第一版无小波结果做消融对比。

### 完整提示词

```text
请在 `/home/wmb/test/波峰波谷预测` 工作。

固定输入：
`实验输出/results/features/model_matrix_seq96_pred336_wavelet.csv`

[待确认] 新增编排代码：
`src/wave_experiments/evaluation/run_wavelet_experiment.py`

[待确认] 输出：
`实验输出/reports/第二版小波实验总结.md`

要求：
1. 使用与第一版相同的 train/val/test 切分。
2. 使用与第一版相同的规则基线和残差目标。
3. 分别训练 LightGBM peak_value、LightGBM peak_hour、XGBoost peak_value、XGBoost peak_hour。
4. 输出与第一版同格式的预测表和评估表。
5. 与无小波版本做消融对比。
6. 判断小波是否改善 peak_value MAE、远 horizon 表现、极端峰值预测、预测分布塌缩问题。

运行命令：
`PYTHONPATH=src /home/wmb/anaconda3/envs/Ada-MSHyper/bin/python src/wave_experiments/evaluation/run_wavelet_experiment.py`

验收标准：
1. LightGBM 和 XGBoost 两类模型均完成。
2. 有无小波消融结果。
3. 明确小波是否保留。
4. `第二版小波实验总结.md` 存在。
```

## 5. 总结

执行时建议严格按顺序推进。每一步提示词都已经包含固定输入路径和验收标准，因此可以单独复制给新的智能体执行。若某一步验收失败，应先修复当前步骤，不要继续堆叠后续模型。
